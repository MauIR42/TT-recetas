import { DatetimeOnePipe } from './datetime-one.pipe';

describe('DatetimeOnePipe', () => {
  it('create an instance', () => {
    const pipe = new DatetimeOnePipe();
    expect(pipe).toBeTruthy();
  });
});
